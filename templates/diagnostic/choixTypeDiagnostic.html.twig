{# templates/diagnostic/choixTypeDiagnostic.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Choix du type de diagnostic{% endblock %}

{% block body %}
    <div class="fixed top-0 left-0">
        <img src="{{ asset('images/EBNtrans.png') }}" alt="EBN Logo" class="size-40">
    </div>

    <div class="fixed top-0 right-0">
        <img src="{{ asset('images/nouveau log 1.png') }}" alt="LCDRLR Logo">
    </div>

    <div class="max-w-lg mx-auto text-center py-20">
        <h1 class="text-4xl font-bold mb-4">Choix du type de diagnostic</h1>
    </div>

    {# Affichage des informations de lieu #}
    <div class="flex flex-row justify-center items-center text-xl font-bold text-blue-600 mt-6 mb-8">
        <div class="mr-4">{{ lieu.nom }}</div>
        <div class="mx-4 text-sm text-gray-600">{{ lieu.nomType }}</div>
        <div class="ml-4 text-sm text-gray-600">{{ lieu.ville }}</div>
    </div>



    {# Création dynamique du tableau avec les types de diagnostics associés à ce lieu #}
    <div class="max-w-lg mx-auto text-center portrait:max-w-2xl">
        <ul>
            {% for diagnostic in typesDiagnostic %}
                <li>
                    <a href="{{ path('app_diagnostic_elements_by_type', {'id': diagnostic.id}) }}" class="bg-teal-500 hover:bg-green-400 text-white rounded-lg px-8 py-3 text-xl shadow transition-colors duration-200 mb-4 flex items-center justify-center w-full portrait:text-4xl">
                        <i class="bi bi-bicycle text-3xl mr-4 portrait:text-6xl"></i>{{ diagnostic.nomType }}</a>
                </li>
            {% endfor %}
        </ul>
    </div>

    {# Bouton retour à l'accueil #}
    <div class="details-container text-center mt-4">
        <a href="{{ path('app_accueil') }}" class="button-style">Retour à l'accueil</a>
    </div>

{% endblock %}
