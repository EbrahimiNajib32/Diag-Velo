{% extends 'base.html.twig' %}

{% block title %}Statistiques{% endblock %}

{% block body %}
    <body class="bg-gray-100 flex flex-col min-h-screen">
    <header class="flex relative w-full justify-between  text-white p-4">
        <div id="headerLogoTissebands">
            <img src="{{ asset('images/EBNtrans.png') }}" alt="EBN Logo" class="size-40">
        </div>
        <div id="headerLogoCite">
            <img src="{{ asset('images/nouveau log 1.png') }}" alt="LCDRLR Logo">
        </div>
    </header>

    <main class="flex-1 p-8">
        <section class="bg-white p-6 rounded-lg shadow-lg mb-8">
            <h2 class="text-2xl font-semibold text-gray-800 mb-4 text-center">Statistiques Générales</h2>


            <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
                <div class="bg-teal-500 text-white p-6 rounded-lg shadow-md text-center">
                    <h3 class="text-lg font-semibold">Utilisateurs Total</h3>
                    <p class="text-2xl">1,254</p>
                </div>

                <div class="bg-teal-500 text-white p-6 rounded-lg shadow-md text-center">
                    <h3 class="text-lg font-semibold">Diagnostics Complétés</h3>
                    <p class="text-2xl">1,254</p>
                </div>

                <div class="bg-teal-500 text-white p-6 rounded-lg shadow-md text-center">
                    <h3 class="text-lg font-semibold">Diagnostics En Cours</h3>
                    <p class="text-2xl">1,254</p>
                </div>

            </div>
        </section>

        <section class="bg-white p-6 rounded-lg shadow-lg mb-8">
            <h2 class="text-2xl font-semibold text-gray-800 mb-4">Graphiques</h2>

            <div class="bg-gray-200 p-4 rounded-lg">
                <p class="text-center">Graphique des statistiques à venir (par exemple, via une bibliothèque JS comme Chart.js)</p>
            </div>
        </section>

        <div class="mt-6 flex justify-center">
            <a href="{{ path('app_accueil') }}" class="bg-teal-500 hover:bg-green-400 text-white rounded-lg px-4 py-2 text-base lg:text-xl shadow transition-colors duration-200 mb-4 flex items-center justify-center">
                <i class="bi bi-box-arrow-left text-xl mr-1"></i>Retour à l'accueil
            </a>
        </div>
    </main>


    </body>
{% endblock %}
