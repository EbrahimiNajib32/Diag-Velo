{% extends 'base.html.twig' %}

{% block title %}Bienvenue{% endblock %}

{% block body %}
    <div class="flex-col-2 flex justify-between">
        <div class="">
            <img src="{{ asset('images/EBNtrans.png') }}" alt="EBN Logo" class="size-40">
        </div>
    </div>
    <div class="fixed top-0 right-0">
        <img src="{{ asset('images/nouveau log 1.png') }}" alt="LCDRLR Logo" class="">
    </div>
    <div class="max-w-lg mx-auto text-center py-20">
        <h1 class="text-4xl font-bold mb-4">Bienvenue</h1>
    </div>
    <div class="text-center portrait:text-3xl mb-6">
        <p>Explorez notre application pour découvrir comment nous sauvons des vélos de la casse et les remettons en état pour une mobilité durable dans la région Mulhousienne !</p>
    </div>

    <div class="max-w-lg mx-auto text-center portrait:max-w-2xl">
        <div class="flex flex-col items-stretch shadow-lg p-6 rounded-lg mt-3">
            <a href="{{ path('velo_new') }}" class="bg-teal-500 hover:bg-green-400 text-white rounded-lg px-8 py-3 text-xl shadow transition-colors duration-200 mb-4 flex items-center justify-center w-full">
                <i class="bi bi-bicycle text-3xl mr-4"></i>
                Saisir un vélo
            </a>
            <a href="/type/diagnostic" class="bg-teal-500 hover:bg-green-400 text-white rounded-lg px-8 py-3 text-xl shadow transition-colors duration-200 mb-4 flex items-center justify-center w-full ">
                <i class="bi bi-card-checklist text-3xl mr-4 "></i>
                Diagnostic vélo
            </a>
            <a href="/diagnosticEnCours" class="bg-teal-500 hover:bg-green-400 text-white rounded-lg px-8 py-3 text-xl shadow transition-colors duration-200 mb-4 flex items-center justify-center w-full ">
                <i class="bi bi-arrow-up-left-circle text-3xl mr-4 "></i>
                Reprendre un diagnostic
            </a>
            <a href="/velo/all" class="bg-teal-500 hover:bg-green-400 text-white rounded-lg px-8 py-3 text-xl shadow transition-colors duration-200 mb-4 flex items-center justify-center w-full">
                <i class="bi bi-search text-3xl mr-4"></i>
                Consulter des vélos
            </a>

            <a href="{{ path('diagnostics_recapitulatif') }}" class="bg-teal-500 hover:bg-green-400 text-white rounded-lg px-8 py-3 text-xl shadow transition-colors duration-200 mb-4 flex items-center justify-center w-full">
                <i class="bi bi-clipboard-data text-3xl mr-4"></i>
                Récapitulatif des Diagnostics
            </a>

        </div>
        {% if app.user %}
            <div class="mb-3">
                Bienvenue {{ app.user.userIdentifier }} ! Votre rôle est :
                {% if is_granted('ROLE_ADMIN') %}
                    Administrateur
                {% elseif is_granted('ROLE_USER') %}
                    Utilisateur
                {% endif %}
            </div>

            <a href="{{ path('app_logout') }}" class="bg-teal-500 hover:bg-green-400 text-white rounded-lg text-xl shadow p-3">
                <i class="bi bi-box-arrow-left text-2xl"></i> Déconnecter
            </a>

            {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path('app_dashboard') }}" class="bg-teal-500 m-3 hover:bg-green-400 text-white rounded-lg text-xl shadow p-3">
                    <i class="bi bi-speedometer text-2xl"></i> Tableau de bord
                </a>
            {% endif %}
        {% else %}
            <div class="mt-3">
                <a href="{{ path('app_login') }}" class="bg-teal-500 hover:bg-green-400 text-white rounded-lg text-xl shadow p-3">
                    <i class="bi bi-box-arrow-in-right text-2xl"></i> Se Connecter
                </a>
            </div>
        {% endif %}
    </div>
{% endblock %}
